spec:
  containers:
  - name: nginx
    image: nginx
    ports:
    - containerPort: 80
    livenessProbe:
      httpGet:
        path: /index.html
        port: 80


Probe 支持以下三种检查方法：

 httpGet
发送HTTP请求，返回200-400范围状态码为成功。

 exec
执行Shell命令返回状态码是0为成功。

 tcpSocket
发起TCP Socket建立成功。
